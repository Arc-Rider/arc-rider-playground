# Apps

This section is for complete application demos where the workflow is the main subject: for example, a project workspace or operations dashboard built with the published arcWidgets package. No app-specific demo has been added yet.

Each future app should document its entry point, npm package version, data source, required environment variables and whether any actions write to external systems.
